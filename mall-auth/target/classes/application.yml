server:
  port: 8401
management:
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  okhttp:
    enabled: true
ribbon:
  ConnectTimeOut: 3000 #服务请求连接超时时间（ms）
  ReadTimeOut: 3000  #服务请求处理超时时间(ms)